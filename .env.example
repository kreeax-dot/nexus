# Copy this file to .env (or .env.local) and fill in your key.
# .env is gitignored; NEVER commit a real key.
#
# Without a key, Growth Agent falls back to a data-aware local mock
# (still supports: task/habit commands, sport tracking, score analysis).

VITE_ANTHROPIC_API_KEY=
